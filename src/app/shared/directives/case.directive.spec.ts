import { CaseDirective } from './case.directive';

describe('CaseDirective', () => {
  it('should create an instance', () => {
    const directive = new CaseDirective();
    expect(directive).toBeTruthy();
  });
});
